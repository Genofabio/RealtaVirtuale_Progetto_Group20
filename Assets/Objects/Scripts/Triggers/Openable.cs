public interface Openable
{
    void ToggleDoor();  // Metodo che alterna lo stato della porta (aperta/chiusa)
    bool IsOpened();      // Metodo che restituisce true se la porta è aperta, false altrimenti
}